<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- namespace bind userdao interface-->
<mapper namespace="com.he.dao.UserDao">
    <select id="getUserList" resultType="com.he.pojo.User">
        select * from user
    </select>
    <select id="getUser" resultType="com.he.pojo.User">
        select * from user where id =#{id};
    </select>
    <insert id="addUser" parameterType="com.he.pojo.User">
        insert into user(`id`,`name`,`pwd`) values(#{id},#{name},#{pwd});
    </insert>
    <insert id="addUser2" parameterType="map">
        insert into user(`id`,`name`,`pwd`) values(#{userId},#{userName},#{userPwd});
    </insert>
    <update id="updateUser" parameterType="com.he.pojo.User">
        update user set pwd=#{pwd},name=#{name} where id =#{id};
    </update>
    <delete id="deleteUser" >
        delete from user where id =#{id};
    </delete>
</mapper>